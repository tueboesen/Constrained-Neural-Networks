
defaults:
  - constraint: water
  - data: water
  - model: equivariant
  - logging: standard

hydra:
  mode: MULTIRUN
  sweep:
    dir: ${logging.hydra_folder}
  sweeper:
    params:
#      +constraints.type: ['','high','low']



run:
  epochs: 10
  debug: false
  seed: 1234
  loss_type: 'eq'
  precision: torch.float64
  device: ???
  loss_indices: ${data.data_id}

optimizer:
  lr: 0.001
  momentum: 0.9

model:
  con_type: ''